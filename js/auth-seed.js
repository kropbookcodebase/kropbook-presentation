/* KROPBOOK — Auth Seed
   Users listed here are seeded into every browser on first visit to the login page.
   This makes credentials available globally across all browsers, devices, and incognito windows.

   HOW TO UPDATE:
   1. Log in as admin and go to Admin Panel → User Registry tab
   2. Click "Export Seed File"
   3. Replace this file (js/auth-seed.js) with the downloaded file
   4. Redeploy to Netlify — all listed users will then work everywhere

   Leave the array empty to fall back to the default admin account only. */
window.KB_AUTH_SEED = [];
