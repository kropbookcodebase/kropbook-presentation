# Claude Instructions

Read and follow `PROJECT_RULES.md` before making any code changes in this repository.

Most important rule: this is a static HTML, CSS, and JavaScript project only. Do not add Next.js, React, TypeScript, TSX/JSX, npm, package managers, build tools, or backend framework code.

The `additional prompts/` folder may contain React or TypeScript examples. Use those files only as visual or interaction references, and convert any useful idea into plain `.html`, `.css`, and `.js` before implementation.
