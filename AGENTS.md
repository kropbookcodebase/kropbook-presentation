# Agent Instructions

Read and follow `.ai-rules/PROJECT_RULES.md` before making any code changes in this repository.

This project is local-first and static: active pages belong in `html/`, styles in `css/`, scripts in `js/`, and root `index.html` is only the entry redirect.

Do not add frameworks, package workflows, external runtime services, or inline CSS/JavaScript.
